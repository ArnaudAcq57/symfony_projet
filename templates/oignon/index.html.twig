{% extends 'base.html.twig' %}

{% block title %}Liste des oignons{% endblock %}

{% block body %}
    <div class="container">
        <h1>🧅 Liste des oignons</h1>
        
        {% for message in app.flashes('success') %}
            <div class="alert alert-success">{{ message }}</div>
        {% endfor %}
        
        <ul class="burger-list">
            {% for oignon in oignons %}
                <li>
                    <span>{{ oignon.name }}</span>
                    <div>
                        <form method="post" action="{{ path('oignon_delete', {'id': oignon.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet oignon ?');" style="display: inline-block;">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oignon.id) }}">
                            <button type="submit" class="btn btn-danger btn-sm">🗑️</button>
                        </form>
                    </div>
                </li>
            {% else %}
                <li>Aucun oignon trouvé.</li>
            {% endfor %}
        </ul>
        
        <div class="nav-links">
            <a href="{{ path('oignon_new') }}" class="btn btn-success">+ Créer un nouvel oignon</a>
            <a href="{{ path('app_burger_list') }}" class="btn btn-back">← Retour aux burgers</a>
        </div>
    </div>
{% endblock %}
